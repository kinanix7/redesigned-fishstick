export interface Statistics {
  totalUsers: number;
  activeUsers: number;
  totalTrips: number;
  activeTrips: number;
  totalRequests: number;
  acceptedRequests: number;
  acceptanceRate: number;
}
